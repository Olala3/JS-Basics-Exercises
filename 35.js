function canPlay() {
  let sport = " Football";

  if (true) {
    var personName = "Cosimo";
  }

  console.log(personName + sport);
}

canPlay();

/* when var is declared inside a scope it can be used any where in the function when let
is declared outside a scope it could be used anywhere, and this is another solution

let personName

function canPlay() {
  let sport = " Football";

  if (true) {
    personName = "Cosimo";
  }

  console.log(personName + sport);
}

canPlay();
*/